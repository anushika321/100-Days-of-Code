#include<stdio.h>
int main()
{
    int a =2;
    int b = 3;
    int c = a+b;
    printf("the value of a is %d and value of b is %d and c is sum of a and b %d", a,b,c);

    printf("the  a divide with b is  %d",a%b);
    return 0;
}